### Tonic - New spreadsheets

Dev Setup:

1. Install Rust and pnpm
2. Install Tauri CLI (`cargo install tauri-cli`)
3. Fetch frontend dependencies (`cd webview-frontend && pnpm install`)
4. Run dev build (`cargo tauri dev`)
